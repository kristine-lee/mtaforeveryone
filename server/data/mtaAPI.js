// const { RESTDataSource } = require('apollo-datasource-rest')

// if (process.env.NODE_ENV !== 'production') require('../../secrets')

// class mtaAPI extends RESTDataSource {
//   constructor(){
//     super()
//     this.baseURL = "https://data.ny.gov/resource/i9wp-a4ja.json/"
//   }
//   async getAccessibleStations({stations}) {
//     const response = await this.get(`?ADA="TRUE"`)
//   }
// }

// //thinking about rendering map on the front end and adding the rest through back end. keep it light
